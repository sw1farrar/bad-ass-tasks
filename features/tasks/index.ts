/**
 * Tasks domain barrel (Kanban, lists, calendar, recurring, dnd).
 *
 * M0 skeleton. Current logic spread in app/page.tsx (inline Kanban), lib/utils (recurring engine), store.
 * Future: extract to here + lib/recurring.ts etc.
 */

export {};
