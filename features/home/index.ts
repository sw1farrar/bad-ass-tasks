/**
 * features/home
 * 
 * Home / Dashboard meta-view (M0 Batch 2.2)
 * 
 * Guard note: All workspace guards, live/demo logic, and data fetching
 * remain in the parent (app/page.tsx) during early M0 batches.
 */
export { HomeView } from "./HomeView";
