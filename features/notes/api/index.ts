/**
 * Notes API / service adapters barrel (M0 prep).
 * Thin wrappers over shared/data/hybridStore for notes (after extraction).
 * All calls must respect live/demo guards.
 *
 * === Agent 1 Parallel Support 2026-05-25 ===
 * Updated comment while monitoring for editor migration writes start.
 * Guard emphasis preserved (critical for post-audit).
 */

export {};
