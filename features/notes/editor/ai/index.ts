/**
 * Editor-specific AI barrel (M0 prep skeleton).
 *
 * Planned: Editor-only AI hooks/transforms (e.g. aiTransform wrappers for slash /ai modes,
 * context builders for note content). Core AI moves to features/ai/ .
 *
 * Imports from @/lib/utils (aiTransformText*) will be refactored to shared/features/ai
 * post-extraction. No hybrid calls here.
 *
 * === Agent 1 (Editor Migration Support) 2026-05-25 ===
 * Barrel prepped and commented. Awaiting migration execution. Guard-safe (no data layer).
 * Ready to accelerate verification once TipTapEditor lands here.
 */

export {};
