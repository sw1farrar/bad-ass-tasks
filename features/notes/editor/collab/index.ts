/**
 * Editor collab primitives (future Yjs/Liveblocks etc, M0 skeleton).
 *
 * Current: Remote cursors UI + onCursorUpdate prop in editor (Agent 30).
 * No CRDT yet; Supabase Realtime presence in store/hybrid (guarded).
 *
 * === Agent 1 Parallel Support Wave - 2026-05-25 ===
 * Barrel updated in support of Batch 1. Monitoring for editor/ TipTapEditor.tsx write.
 * Post-land: re-audit that collab stubs don't touch guards.
 */

export {};
