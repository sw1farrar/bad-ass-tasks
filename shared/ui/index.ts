/**
 * Shared UI / design system barrel (M0 prep).
 *
 * Glass primitives, buttons, badges, priority indicators, etc.
 * (Many currently inline Tailwind in components/page; extract here for reuse.)
 *
 * Avoid putting feature-specific composites here (those go to shared/components/ or feature/components).
 */

export {};
