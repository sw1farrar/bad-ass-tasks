/**
 * Shared state barrel (M0 prep).
 *
 * Future: Lighter Zustand slices (per feature or UI-only), TanStack Query setup,
 * instead of monolithic store/useTaskStore.ts .
 *
 * Keep hybrid optimistic + realtime in sync.
 */

export {};
