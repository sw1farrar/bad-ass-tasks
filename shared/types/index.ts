/**
 * Shared types barrel (M0 prep).
 *
 * Target: All domain types (Task, Note with linked_* , Workspace, etc.) + supabase.ts DB types
 * moved here from types/ and used via @/shared/types .
 *
 * Current types/ at root still active (update imports gradually).
 */

export {};
