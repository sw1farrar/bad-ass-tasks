/**
 * Shared Supabase clients barrel (M0 prep).
 *
 * Thin clients + auth helpers. Current home: lib/supabase/{client,server}.ts
 * (isSupabaseConfigured lives here; used by hybrid guards).
 *
 * Migration: move files here, update imports (additive reexports possible).
 */

export {};
