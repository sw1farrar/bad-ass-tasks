/**
 * Shared pure utils barrel (M0 prep).
 *
 * Target: cn, formatters, date helpers from lib/utils.ts .
 * Recurring engine + AI layer move OUT to features/tasks/lib + features/ai/ .
 * Keep this for pure, no side-effect, no data access functions.
 */

export {};
