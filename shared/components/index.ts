/**
 * Shared components barrel (M0 prep).
 *
 * Only TRULY cross-cutting composites: CommandPalette, Confetti, ErrorBoundary,
 * SupabaseSetupBanner, etc. (from current components/).
 *
 * Feature-specific (TaskModal, AIChatPanel, KnowledgeGraph, TipTap) move to their features/.
 */

export {};
